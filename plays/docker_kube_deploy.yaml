---
# Playbook for deploying docker + kubernetes on target hosts

 - name: Docker + Kubernetes install
   become: yes
   hosts:
     dev
   tasks:

   - name: Install Docker
     apt:
      name: docker
      force_apt_get: yes

   - name: Install Kubernetes
     apt:
      name: kubernetes
      force_apt_get: yes